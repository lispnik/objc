# macOS
.DS_Store

# Build artifacts
/bin/
*.fasl
*.fas
*.lx64fsl
*.dx64fsl
*.64yfasl

# ocicl-vendored dependency sources -- restore with `ocicl install`.
# The lockfile (ocicl.csv) is committed; the sources are not.
/ocicl/

# Example app bundles
examples/notes-app/build/

# Swift bridge build product
examples/swift/libLispSwift.dylib
